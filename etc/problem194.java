
import java.util.ArrayList;

public class problem194 {
  public static final int MODULO = 100_000_000;

  public static class ConfigurationCounter {
    // The number of possible color configurations for Unit A,
    // assuming the two vertices on the far left are of fixed,
    // distinct colors.
    public static int unitAColors(int colorCount) {

    }

    // Configuration I: All four corners are of different colors.
    //
    //  w---x
    //  |\ /|
    //  | ? |
    //  | | |
    //  | ? |
    //  | | |
    //  | ? |
    //  |/ \|
    //  y---z
    private static int config1Colors(int colorCount) {
      int total = 0;
      // Case I.i: Assume 
    }
  }

  public static void main(String[] args) {
    System.out.println(MODULO);
  }
}
